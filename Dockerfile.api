FROM python:3.9-slim

WORKDIR /app

# Copy service files
COPY combined_api.py job_status.py ./

RUN pip install fastapi uvicorn python-multipart requests pydantic

CMD ["uvicorn", "combined_api:app", "--host", "0.0.0.0", "--port", "8002"]